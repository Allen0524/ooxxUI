// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
	/* config options here */
	experimental: {appDir: true},
	images: {
		domains: [''],
	},
};

module.exports = nextConfig;
